const database = require("../database/menu");
/*
const getAllMenu = () =>{
  const allMenu = database.getAllMenu();
  return allMenu;
};

const getOneMenu = () => {
  return;
}

const createNewMenu = (newMenu) => {
  const menuToInsert = {
    ...newMenu,
    id: (),
    createdAT: new

  }
}
*/
